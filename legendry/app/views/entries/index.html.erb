<h1>Specification</h1>

<table>
  <tr>
    <th></th>
  </tr>

  <% @entries.each do |entry| %>
    <tr>
      <td><%= entry.title %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New entry', new_organization_specification_entry_path %>
